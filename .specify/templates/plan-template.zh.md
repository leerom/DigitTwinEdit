# 实施计划: [功能名称]

**分支**: `[###-feature-name]` | **日期**: [日期] | **规格**: [链接]
**输入**: 来自 `/specs/[###-feature-name]/spec.md` 的功能规格

**注意**: 此模板由 `/speckit.plan` 命令填充。有关执行工作流程,请参阅 `.specify/templates/commands/plan.md`。

## 概要

[从功能规格提取:主要需求 + 研究中的技术方法]

## 技术上下文

<!--
  需要执行的操作:用项目的技术细节替换此部分的内容。
  此处呈现的结构以建议的方式指导迭代过程。
-->

**语言/版本**: [例如,Python 3.11、Swift 5.9、Rust 1.75 或 需要澄清]
**主要依赖**: [例如,FastAPI、UIKit、LLVM 或 需要澄清]
**存储**: [如果适用,例如,PostgreSQL、CoreData、文件 或 不适用]
**测试**: [例如,pytest、XCTest、cargo test 或 需要澄清]
**目标平台**: [例如,Linux 服务器、iOS 15+、WASM 或 需要澄清]
**项目类型**: [单体/Web/移动 - 决定源代码结构]
**性能目标**: [特定领域,例如,1000 请求/秒、10k 行/秒、60 fps 或 需要澄清]
**约束条件**: [特定领域,例如,<200ms p95、<100MB 内存、离线可用 或 需要澄清]
**规模/范围**: [特定领域,例如,1万用户、100万代码行、50个屏幕 或 需要澄清]

## Constitution 检查

*门禁:在阶段 0 研究之前必须通过。阶段 1 设计后重新检查。*

[根据 constitution 文件确定的门禁]

### 中文优先检查
- ✅ 所有文档使用中文编写
- ✅ 代码注释使用中文(除非项目规范要求英文)
- ✅ 用户界面文本使用中文
- ✅ 错误信息和日志使用中文

### 原生优先检查
- [ ] 已评估原生 API 方案
- [ ] 第三方库引入已充分说明理由

### TDD 检查
- [ ] 测试先行策略已确认
- [ ] 测试覆盖率目标 ≥ 90%

### 性能检查
- [ ] UI 响应时间目标 ≤ 100ms
- [ ] 渲染帧率目标 ≥ 60fps

### 架构检查
- [ ] 设计模式已选择并说明理由
- [ ] ECS 架构已应用(如适用)

### 简单性检查
- [ ] MVP 范围已明确
- [ ] 避免过早抽象

## 项目结构

### 文档(此功能)

```text
specs/[###-feature]/
├── plan.md              # 本文件 (/speckit.plan 命令输出)
├── research.md          # 阶段 0 输出 (/speckit.plan 命令)
├── data-model.md        # 阶段 1 输出 (/speckit.plan 命令)
├── quickstart.md        # 阶段 1 输出 (/speckit.plan 命令)
├── contracts/           # 阶段 1 输出 (/speckit.plan 命令)
└── tasks.md             # 阶段 2 输出 (/speckit.tasks 命令 - 不由 /speckit.plan 创建)
```

### 源代码(仓库根目录)
<!--
  需要执行的操作:用此功能的具体布局替换下面的占位符树。
  删除未使用的选项,并使用实际路径扩展所选结构
  (例如,apps/admin、packages/something)。交付的计划不得包含选项标签。
-->

```text
# [如果未使用则删除] 选项 1: 单一项目 (默认)
src/
├── models/
├── services/
├── cli/
└── lib/

tests/
├── contract/
├── integration/
└── unit/

# [如果未使用则删除] 选项 2: Web 应用程序 (检测到 "frontend" + "backend" 时)
backend/
├── src/
│   ├── models/
│   ├── services/
│   └── api/
└── tests/

frontend/
├── src/
│   ├── components/
│   ├── pages/
│   └── services/
└── tests/

# [如果未使用则删除] 选项 3: 移动 + API (检测到 "iOS/Android" 时)
api/
└── [与上述 backend 相同]

ios/ 或 android/
└── [平台特定结构:功能模块、UI 流程、平台测试]
```

**结构决策**: [记录所选结构并引用上面捕获的实际目录]

## 复杂度跟踪

> **仅当 Constitution 检查有必须说明理由的违规时才填写**

| 违规项 | 为什么需要 | 拒绝更简单替代方案的原因 |
|--------|-----------|------------------------|
| [例如,第 4 个项目] | [当前需求] | [为什么 3 个项目不够] |
| [例如,Repository 模式] | [具体问题] | [为什么直接 DB 访问不够] |

## 阶段划分

### 阶段 0: 研究
- 研究现有代码库中的类似实现
- 评估技术方案可行性
- 确定设计模式和架构选择

### 阶段 1: 设计
- 完成数据模型设计
- 定义 API 契约
- 编写快速入门指南

### 阶段 2: 实施
- 参考 tasks.md 进行任务分解
- 遵循 TDD 流程
- 增量提交

### 阶段 3: 验证
- 运行测试套件
- 性能基准测试
- 用户验收测试

## 风险与缓解

| 风险 | 影响 | 可能性 | 缓解措施 |
|------|------|--------|----------|
| [技术风险] | [高/中/低] | [高/中/低] | [缓解策略] |

## 参考文档

- Constitution: `.specify/memory/constitution.md`
- 项目概述: `CLAUDE.md`
- 原始需求: `rawRequirements/`
